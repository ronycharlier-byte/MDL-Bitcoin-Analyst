# Models Directory

Reserved for persisted calibration artifacts, trained parameter snapshots, or approved model configuration exports.

Current implementation is code-first and stores run metadata in `data/quant_model.db` plus JSON summaries in `data/simulations`.
