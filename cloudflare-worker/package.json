{
  "name": "quant-btc-model-worker",
  "version": "1.16.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:schema": "powershell -ExecutionPolicy Bypass -File ./deploy_and_write_schema.ps1",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260504.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.50.0"
  }
}
